# Non-secret local defaults only.
ZHIXU_LOG_LEVEL=INFO
ZHIXU_DATA_DIR=

# Credentials, external identifiers, vault passphrases, and deployment targets
# must not be stored in this file. Configure them through their dedicated,
# access-controlled setup flows.
